# Ακρίβεια: από «εξαφάνιση πρόβλεψης» σε GPS

Σημειώσεις της τοπικής δουλειάς. **Τίποτα δεν έχει μπει σε git ή στον VPS.**
Όλες οι αλλαγές είναι προσθετικές και η προεπιλεγμένη συμπεριφορά είναι
ακριβώς η σημερινή — το GPS ανάβει μόνο με ρητή μεταβλητή περιβάλλοντος.

---

## 1. Γιατί έβγαζε 403 το getBusLocation

Δεν έφταιγε το endpoint. Έφταιγε ο τρόπος κλήσης.

| | getStopArrivals (δούλευε) | getBusLocation (έβγαζε 403) |
|---|---|---|
| προσπάθειες | `attempts=1` | `MAX_RETRIES=4` |
| σε 403 | `retry_forbidden=False` → σταματά | `True` → ξαναχτυπά με backoff |
| timeout | 5 s | 20 s |
| όριο ρυθμού | `RateLimiter` στον `_stop_worker` | **κανένα** στο `batch_get_bus_locations` |

Δηλαδή το GPS μονοπάτι έριχνε 715 κλήσεις όσο γρήγορα προλάβαιναν 16 νήματα, και
σε κάθε 403 απαντούσε με **τέσσερις ακόμη κλήσεις** — ενίσχυση τη στιγμή που ο
server ζητούσε υποχώρηση.

**Ελεγχόμενο πείραμα** (`scripts/probe_rate_limits.py`, ίδιος host, ίδιοι ρυθμοί,
χωρίς retries):

| ρυθμός | getStopArrivals %403 | getBusLocation %403 |
|-------:|---------------------:|--------------------:|
| 10/s | 0,00 % | 0,00 % |
| 20/s | 0,17 % | 0,99 % |
| 30/s | 1,11 % | 1,33 % |
| 45/s | **4,28 %** | **2,44 %** |

Τα δύο endpoints συμπεριφέρονται ίδια — στον υψηλότερο ρυθμό το getBusLocation
ήταν μάλιστα **καλύτερο**.

> **Παράπλευρο εύρημα:** το σημερινό `TARGET_RATE = 55` είναι πάνω από την καθαρή
> ζώνη. Ένα αισθητό ποσοστό κλήσεων απορρίπτεται ήδη σήμερα, και **κάθε χαμένη
> κλήση είναι χαμένη ευκαιρία να δεις μια εξαφάνιση, άρα χαμένη διέλευση**. Ο
> υψηλός ρυθμός κοστίζει δεδομένα αντί να τα αγοράζει.

⚠️ Το όριο είναι **ανά IP**. Τρέξε το probe **και στον VPS** πριν αλλάξεις ρυθμούς.

---

## 2. Τι μετρήθηκε

### Γεωμετρία διαδρομής (15 διαδρομές, όλες καθαρές)

Η πολυγραμμή από `webGetRoutesDetailsAndStops` αναπαράγει την **ίδια τη δηλωμένη
απόσταση του ΟΑΣΑ με σφάλμα 0,0–0,1 %**. Ανεξάρτητη επιβεβαίωση ότι η γεωμετρία
και τα σωρευτικά μήκη είναι σωστά.

Η σημερινή προσέγγιση (ευθείες μεταξύ στάσεων, `_stop_distances`) **υπο-μετρά
κατά 9,0 % κατά μέσο όρο, έως 23,4 %** (τρόλεϊ 040). Αυτό το σφάλμα είναι ο
άξονας x κάθε παλινδρόμησης και κάθε σταθμισμένης-με-απόσταση εκτίμησης.

### Κάλυψη (κοινό παράθυρο 44 λεπτών, 15 διαδρομές)

| μέθοδος | διελεύσεις | ανά λεπτό | στάσεις | οχήματα |
|---|---:|---:|---:|---:|
| GPS | 1.406 | **31,8** | **502** | 62 |
| εξαφάνιση | 186 | 4,2 | 66 | 49 |

**7,6× διελεύσεις, 7,6× στάσεις.** Και η κατανομή είναι το ουσιώδες: η εξαφάνιση
έδωσε **0** διελεύσεις στη μέση της διαδρομής (202 στα άκρα, 0 στη μέση), το GPS
έδωσε 1.415.

### Φυσική ευλογοφάνεια — το πιο καθαρό αποτέλεσμα

Ταχύτητες που υπονοούνται ανάμεσα σε διαδοχικές στάσεις:

| μέθοδος | τμήματα | διάμεσος | p95 | **> 80 km/h** |
|---|---:|---:|---:|---:|
| GPS | 1.476 | 15,2 km/h | 30,9 km/h | **0,3 %** |
| εξαφάνιση | 108 | 17,6 km/h | **138,1 km/h** | **11,1 %** |

Ένα αστικό λεωφορείο δεν πιάνει 138 km/h μέσο όρο ανάμεσα σε δύο στάσεις. Η
μέθοδος εξαφάνισης αυτοαναιρείται στο **11 %** των τμημάτων.

### Συστηματική μεροληψία — επιβεβαιώθηκε με μέτρηση

Σε **118 ζεύγη** που περιγράφουν την ίδια φυσική διέλευση:

```
διάμεσος διαφορά (GPS − εξαφάνιση) = −20,2 s     μέση = −27,2 s
εντός ±30 s: 67 %   ±60 s: 91 %   ±120 s: 96 %
```

Δηλαδή **η ανίχνευση εξαφάνισης τοποθετεί κάθε διέλευση ~20 δευτερόλεπτα
ΑΡΓΟΤΕΡΑ** από ό,τι πραγματικά συνέβη. Δεν είναι θόρυβος: το πρόσημο είναι
αρνητικό **και στις 10 διαδρομές** (−9 s έως −44 s). Αυτό ακριβώς προβλέπει η
μηχανική της μεθόδου — η διέλευση καρφώνεται μέσα στο παράθυρο [τελευταία θέαση,
πρώτη απουσία], που είναι ένας ολόκληρος κύκλος δημοσκόπησης πλατύ.

Επειδή είναι **σταθερή**, είναι και αφαιρέσιμη — αλλά μόνο αν την ξέρεις.

### Η αναχώρηση — το κρίσιμο σημείο

Στις διαδρομές του δείγματος η μέθοδος εξαφάνισης κατέγραψε **μηδέν** διελεύσεις
στην ίδια την αφετηρία. Δεν είναι ατυχία· είναι δομικό, και το λέει ήδη ο κώδικας:
*«the origin itself gives no arrival prediction on non-circular routes»*. Άρα
**κάθε αναχώρηση είναι σήμερα υπολογισμένη, όχι μετρημένη**.

Το GPS τις μετράει απευθείας. Επαλήθευση με **ανεξάρτητη πηγή** — το πρόγραμμα:

```
n=9   διάμεσος απόκλισης −0,3′   εύρος −6,0′ … +2,5′
```

Αν η προβολή ή η παρεμβολή ήταν λάθος, οι ώρες δεν θα έπεφταν πάνω στο πρόγραμμα.

---

## 3. Τι άλλαξε

### Νέα αρχεία

| αρχείο | τι κάνει |
|---|---|
| `scripts/geo.py` | προβολή στίγματος σε πολυγραμμή· ισοαπέχουσα προβολή με συντελεστές στο μέσο πλάτος κάθε διαδρομής |
| `scripts/sync_shapes.py` | κατεβάζει γεωμετρία, τοποθετεί τις στάσεις πάνω της |
| `scripts/gps_tracker.py` | βρόχος getBusLocation + παραγωγή διελεύσεων **στη ροή** |
| `scripts/compare_methods.py` | σύγκριση των δύο μεθόδων στα ίδια δρομολόγια |
| `scripts/probe_rate_limits.py` | μέτρηση ορίων — **τρέξε το στον VPS** |
| `scripts/test_gps_math.py` | έλεγχοι γεωμετρίας με γνωστή σωστή απάντηση |

### Νέοι πίνακες

`route_shapes` (πολυγραμμή + σωρευτικά μέτρα), `stop_shape_offsets` (θέση κάθε
στάσης πάνω στην πολυγραμμή), στήλες `stop_passages.method` και
`vehicle_pings.heading`.

### Διορθώσεις σε υπάρχοντα

- **`slot_handoffs` μεγάλωνε για πάντα** — καμία διαδρομή του κώδικα δεν το
  καθάριζε ποτέ, ενώ το compute τρέχει ~96 φορές τη μέρα και ξαναέγραφε τα ίδια.
  Δοκιμή σε προσομοίωση της κατάστασης του VPS: **2.496 → 26 σειρές (−99,0 %)**.
- **Παγωμένο token bucket** — `cap = min(rate, BURST_CAP)`· με ρυθμό < 1/s το
  κουπόνι δεν φτάνει ποτέ το κατώφλι της `acquire()` και ο poller κολλάει
  σιωπηλά. Υπήρχε **και στον υπάρχοντα `RateLimiter`**· δεν είχε φανεί μόνο
  επειδή το `TARGET_RATE` ήταν πάντα ≥ 25. Με ρυθμιζόμενη τιμή, γίνεται εφικτό.
- **Ημερολογιακή vs ημέρα βάρδιας** — το `sync_schedules` έγραφε `date.today()`
  ενώ όλα τα υπόλοιπα διαβάζουν `athens_service_date()`. Διαφέρουν 00:00–04:00.
- **Νεκρή τριπλή σύγκριση** — το `normal_rows` ήταν σταθερά `0`. Υλοποιήθηκε.
  Πρώτο αποτέλεσμα: **106 θεωρητικές αναχωρήσεις χωρίς ημερήσιο αντίστοιχο**,
  η γραμμή 608 χάνει ~20 %. Προσοχή: μέρος αυτού είναι μετατόπιση ωρών, όχι
  καθαρή περικοπή.
- **Καμία ρύθμιση ρυθμού στο `sync_master_data`** — προστέθηκε (8/s), μαζί με
  `--lines` για υποσύνολο.

### Διασταυρούμενη μόλυνση (θα περνούσε απαρατήρητη)

Τρία σημεία διάβαζαν `stop_passages` χωρίς φίλτρο μεθόδου:

- `local_poller.measure_origin_yield` — το GPS παράγει διελεύσεις για **κάθε**
  στάση, οπότε μια νεκρή αφετηρία θα φαινόταν ξαφνικά παραγωγική και ο poller θα
  ξόδευε πάλι χιλιάδες κλήσεις τη μέρα σε στάση που δεν απαντά ποτέ.
- `rotation_slots._accumulate_segment_times` — οι «μαθημένοι» χρόνοι τμημάτων θα
  γίνονταν μείγμα δύο μετρήσεων, και τροφοδοτούν πίσω στις εκτιμήσεις.
- `compute_daily_report._routes_needing_recompute` — κάθε γραφή GPS θα σήμαινε
  τη διαδρομή μπαγιάτικη, δηλαδή πλήρης επανυπολογισμός κάθε 15 λεπτά.

---

## 4. Πώς δοκιμάζεται

```bash
python scripts/test_gps_math.py          # γεωμετρία, χωρίς δίκτυο
python scripts/probe_rate_limits.py      # όρια — ΤΡΕΞΕ ΤΟ ΣΤΟΝ VPS
python scripts/sync_shapes.py            # γεωμετρία διαδρομών
python scripts/gps_tracker.py --minutes 60 --rate 18
python scripts/compare_methods.py --reconstruct
```

## 5. Πώς ανάβει (όταν αποφασιστεί)

```bash
ATHENSBUS_GPS_RATE=18      # GPS· 0 = εκτός (προεπιλογή)
ATHENSBUS_TARGET_RATE=5    # οι στάσεις γίνονται δεύτερη γνώμη
ATHENSBUS_PASSAGE_SOURCE=gps   # disappearance (προεπιλογή) | gps | both
```

Σύνολο 23/s αντί για 55/s: **λιγότερα από τα μισά αιτήματα**, με κάλυψη όλων των
στάσεων αντί για 6 ανά διαδρομή.

---

## 5β. Ανακατασκευή δρομολογίων — και μια ΠΑΛΙΝΔΡΟΜΗΣΗ

| πηγή | δρομ. | μετρημένη αναχώρηση | με λήξη | σημεία/δρομ. | αδύνατα |
|---|---:|---:|---:|---:|---:|
| εξαφάνιση | 82 | **0 (0 %)** | 43 (52 %) | 2,4 | 42 |
| GPS | 97 | **35 (36 %)** | 64 (66 %) | **16,2** | 34 |
| και τα δύο | 105 | 35 (33 %) | 70 (67 %) | 15,8 | 37 |

Θετικά: **6,75× περισσότερα σημεία ανά δρομολόγιο**, η μετρημένη αναχώρηση από
0 % σε 36 %, η λήξη από 52 % σε 66 %, και το `no_departure_observed` από 41/82
(50 %) σε 25/97 (26 %).

⚠️ **Αλλά προσοχή — δύο δείκτες ΧΕΙΡΟΤΕΡΕΨΑΝ με GPS:**

| | εξαφάνιση | GPS |
|---|---:|---:|
| `departure_inversion` | 1 | **6** |
| `vehicle_overlap` | 0 | **3** |

Αυτό **δεν** σημαίνει ότι το GPS είναι λιγότερο ακριβές — οι ώρες του
επαληθεύονται ανεξάρτητα από το πρόγραμμα και από τη φυσική. Σημαίνει ότι **η
ανακατασκευή δεν είναι φτιαγμένη για πυκνά δεδομένα**. Το `_split_trips` και τα
δίχτυα του (`LOOP_DWELL_MINS`, `TERMINAL_CLUSTER`, `young_limit`, `mature_mins`,
`MIN_DURATION_FRACTION`) συντονίστηκαν για 2,4 σημεία ανά δρομολόγιο με τρύπα στη
μέση. Δώσε τους 16 σημεία και οι κανόνες «οπισθοχώρησης» και «ζώνης τερματικού»
αρχίζουν να κόβουν εκεί που δεν πρέπει.

**Αυτό είναι το επόμενο κομμάτι δουλειάς, και πρέπει να γίνει ΠΡΙΝ τη μεταγωγή.**

---

## 6. Τι ΔΕΝ έχει αποδειχθεί ακόμη

Η σύγκριση έτρεξε σε **44 λεπτά κοινού παραθύρου, 15 διαδρομές**. Στέρεα από αυτό
το δείγμα: η γεωμετρία, η φυσική ευλογοφάνεια, η κάλυψη, η μεροληψία των 20 s,
η επαλήθευση με το πρόγραμμα. **Δεν** είναι στέρεο τίποτα σε επίπεδο ολόκληρου
δρομολογίου — ένα δρομολόγιο θέλει 40–90 λεπτά, οπότε τα περισσότερα ήταν κομμένα
εξ ορισμού, και οι παραπάνω παλινδρομήσεις μπορεί να είναι είτε πραγματικές είτε
τεχνούργημα των κομμένων δρομολογίων. Χρειάζεται πλήρης ημέρα για να ξεχωρίσει.

Πριν από οποιαδήποτε μεταγωγή:

1. Τρέξε το probe **στον VPS** (άλλη IP, άλλο όριο).
2. Μάζεψε **μία πλήρη ημέρα** με τις δύο μεθόδους παράλληλα.
3. `compare_methods.py --reconstruct` — σύγκριση αναχώρησης/λήξης/διάρκειας.
4. Μόνο τότε `ATHENSBUS_PASSAGE_SOURCE=gps`.

Επίσης ανοιχτό: με πλήρη κάλυψη GPS, ένα μεγάλο μέρος της ευρετικής στο
`_split_trips` (`LOOP_DWELL_MINS`, `TERMINAL_CLUSTER`, `young_limit`,
`mature_mins`, `MIN_DURATION_FRACTION`) υπάρχει **μόνο** επειδή λείπει η μέση της
διαδρομής. Με GPS δεν λείπει. Αυτά τα δίχτυα δεν πρέπει να αφαιρεθούν στα τυφλά —
πρέπει να αφαιρεθούν **αφού** μετρηθεί ότι δεν πιάνουν τίποτα πια.
